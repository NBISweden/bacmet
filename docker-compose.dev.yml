services:
  app:
    build:
      target: dev
    volumes:
      - ./app/app:/opt/bacmet/app

  frontend:
    build:
      dockerfile: frontend/Dockerfile
    volumes:
      - ./frontend/bacmet:/opt/bacmet/frontend
